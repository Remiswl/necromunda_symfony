{{ form_start(form) }}

	{{ form_row(form.name) }}
	{{ form_row(form.income) }}

	<div class="d-flex justify-content-between">
		{{ form_row(form.mind66) }}
		{{ form_row(form.maxd66) }}
	</div>

	{{ form_row(form.description) }}

	<button class="btn btn-primary offset-4">Add territory</button>

{{ form_end(form) }}