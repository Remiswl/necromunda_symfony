{% extends 'base.html.twig' %}

{% block title %}New skill{% endblock %}

{% block body %}

	<section id="new_skill" class="mb-5">
		<div class="col-3 m-auto text-white">

			{{ form_start(form) }}

				<div>{{ form_row(form.name) }}</div>
				<div>{{ form_row(form.description) }}</div>
				<div>{{ form_row(form.category) }}</div>

				<button class="btn btn-primary offset-4">Add skill</button>
				<a href="{{ path('settings') }}" class="btn btn-secondary my-2">Back</a>

			{{ form_end(form) }}

		</div>
	</section>

{% endblock %}